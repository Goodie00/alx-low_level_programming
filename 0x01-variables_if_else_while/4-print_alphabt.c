#include <stdio.h>
/**
 * Description: using the main function
 * this program prints "Programming is positive, zero or negative"
 * Return: 0
 */
int main (void)
{
	char low, e, q;

	e = 'e';
	q = 'q';

	for (c = 'a'; c <= 'z'; c++)
	
		if (c != 'e' && c != 'q')
		{	
	putchar(c);
		}
}
putchar('\n');
return (0);
}

